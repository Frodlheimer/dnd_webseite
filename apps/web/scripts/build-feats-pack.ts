import { existsSync, mkdirSync, readdirSync, readFileSync, rmSync, writeFileSync } from 'node:fs';
import path from 'node:path';
import { fileURLToPath } from 'node:url';

import {
  buildFeatsPackFromContentEntries,
  type SourceFeatJson
} from '../src/rules/feats/parse/parseFeatsContentJson';

const __filename = fileURLToPath(import.meta.url);
const __dirname = path.dirname(__filename);
const repoRoot = path.resolve(__dirname, '../../..');

const envDirRaw = process.env.FEATS_JSON_DIR?.trim();
const envDir = envDirRaw ? path.resolve(envDirRaw) : null;
const fallbackDir = path.resolve(repoRoot, 'content');

const indexOutputPath = path.resolve(repoRoot, 'apps/web/src/rules/feats/generated/featsIndex.ts');
const detailsOutputDir = path.resolve(repoRoot, 'apps/web/public/rules/feats/entries');

const resolveInputDir = (): string => {
  if (envDir) {
    return envDir;
  }

  return fallbackDir;
};

const clearDirectoryJsonFiles = (directoryPath: string): void => {
  if (!existsSync(directoryPath)) {
    return;
  }

  const entries = readdirSync(directoryPath, {
    withFileTypes: true
  });
  for (const entry of entries) {
    const fullPath = path.join(directoryPath, entry.name);
    if (entry.isDirectory()) {
      clearDirectoryJsonFiles(fullPath);
      continue;
    }

    if (entry.isFile() && entry.name.toLowerCase().endsWith('.json')) {
      rmSync(fullPath, {
        force: true
      });
    }
  }
};

const loadEntries = (inputDir: string): Array<{ fileName: string; payload: SourceFeatJson }> => {
  return readdirSync(inputDir)
    .filter((entry) => entry.toLowerCase().endsWith('.json') && entry.toLowerCase() !== '_index.json')
    .sort((left, right) => left.localeCompare(right))
    .map((fileName) => {
      const payload = JSON.parse(readFileSync(path.resolve(inputDir, fileName), 'utf8')) as SourceFeatJson;
      return {
        fileName,
        payload
      };
    })
    .filter((entry) => entry.payload?.kind === 'FEAT' && Array.isArray(entry.payload.content?.tree));
};

const run = async (): Promise<void> => {
  const inputDir = resolveInputDir();
  if (!existsSync(inputDir)) {
    const lines: string[] = [];
    lines.push('[feats:build] Failed to build feats pack.');
    lines.push(`FEATS_JSON_DIR: ${envDirRaw ? envDirRaw : '(not set)'}`);
    lines.push(`Tried input dir: ${inputDir}`);
    lines.push(`Fallback dir: ${fallbackDir}`);
    lines.push('Provide FEATS_JSON_DIR or place feat JSON files in ./content.');
    throw new Error(lines.join('\n'));
  }

  const entries = loadEntries(inputDir);
  if (entries.length === 0) {
    throw new Error(`[feats:build] No valid FEAT JSON files found in ${inputDir}.`);
  }

  const pack = buildFeatsPackFromContentEntries(entries);

  mkdirSync(path.dirname(indexOutputPath), {
    recursive: true
  });
  mkdirSync(detailsOutputDir, {
    recursive: true
  });
  clearDirectoryJsonFiles(detailsOutputDir);

  for (const detail of pack.details) {
    const outputPath = path.resolve(detailsOutputDir, `${detail.id}.json`);
    writeFileSync(outputPath, JSON.stringify(detail, null, 2), 'utf8');
  }

  const banner =
    '// This file is auto-generated by apps/web/scripts/build-feats-pack.ts.\n' +
    '// Do not edit manually.\n\n';
  const source =
    `${banner}` +
    "import type { FeatsPackIndex } from '../types';\n\n" +
    `export const featsPackIndex: FeatsPackIndex = ${JSON.stringify(pack.index, null, 2)};\n`;

  writeFileSync(indexOutputPath, source, 'utf8');

  console.log(`[feats:build] Built feats pack with ${pack.index.count} entries.`);
  console.log(`[feats:build] Input dir: ${inputDir}`);
  console.log(`[feats:build] Index output: ${indexOutputPath}`);
  console.log(`[feats:build] Detail output: ${detailsOutputDir}`);
};

void run().catch((error) => {
  const message = error instanceof Error ? error.message : String(error);
  console.error(message);
  process.exit(1);
});
